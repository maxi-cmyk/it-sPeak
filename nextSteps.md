# Plan

Build the PRD's project-folder and progress-tracking system on top of the working multimodal analysis pipeline. The implementation should first make projects, sessions, videos, score snapshots, and coaching cards durable; then add five-session replacement rules, baseline-relative progress, comparable-session safeguards, replay, stagnation alerts, and the coaching playbook without changing the existing CV or audio algorithms.

## Scope
- In: authenticated project ownership, Supabase persistence, Cloudflare R2 video retention, named project folders, five-session enforcement, protected calibration baselines, user-controlled replacement, progress calculations, archetype comparability, stagnation detection, best-session replay, saved coaching cards, frontend workflows, migrations, and automated verification.
- Out: retraining or retuning visual/audio models, implementing the remaining archetype presets or recommendation quiz, billing, team collaboration, native mobile clients, and unrelated production deployment work.

## Action items
[~] **Define the durable project and session domain model before changing UI behavior — in progress.** Done: the versioned Supabase migration and matching SQL snapshot now define profiles, projects, sessions, analysis results, coaching cards, lifecycle events, ownership constraints, immutable per-generation sequence numbers, baseline references, replacement targets, score snapshots, confidence, reports, and timestamps. Remaining: add `playbook_entries` and finish the fields and constraints needed by replay/playbook behavior.

[~] **Implement Supabase and R2 adapters behind the existing persistence ports — in progress.** Done: `backend/itspeak/persistence.py` has in-memory and Supabase persistence implementations, private Supabase Storage uploads, signed artifact URLs, ownership-scoped queries, temporary artifact cleanup, and an applied forward migration correcting the deployed `digest()` function search path; the live function configuration and pgcrypto digest call are verified. Remaining: confirm a successful full analysis commit and complete the originally scoped R2 adapter/lifecycle, authenticated deletion, and recovery/reconciliation paths.

[~] **Add authenticated project-folder APIs and replace frontend mock data — in progress.** Done: project create/list/read/update/delete, project session listing, session detail/artifact endpoints, validation, and the API-backed dashboard/project UI with loading, empty, error, and mutation states are implemented; fixture project imports are removed. The frontend now sends a fresh Clerk session token on every FastAPI call, and FastAPI verifies it with Clerk before using its `sub` claim for ownership. Remaining: verify real two-user isolation end to end and add the reset endpoint.

[~] **Enforce the five-retained-session rule as a server-side transaction — in progress.** Done: the in-memory adapter and Supabase transaction require replacement at five retained sessions, exclude Session 1, validate ownership/state/generation, preserve the old session until successful analysis, assign immutable Session 6, and expose the replacement flow in the frontend; core boundary tests pass and the live database dependency that blocked commits is fixed. Remaining: rerun a full live analysis commit, finish stale/concurrent and failed-analysis coverage, complete cleanup after commit, show every specified replacement dependency, and run the full Sessions 1-6 scenario.

[~] **Protect Session 1 and implement explicit project reset semantics — in progress.** Done: the first successful commit becomes the baseline, database and adapter guards prevent baseline replacement, and failed/rejected uploads do not receive a retained sequence number. Remaining: implement the separately authenticated reset API, destructive confirmation, generation transition, linked cleanup, and reset/aborted-upload integration coverage.

[~] **Create one progress service with explicit comparability rules — in progress.** Done: durable reports retain absolute archetype-calibrated scores, aggregate scores, metric confidence, archetype keys, and scoring-config versions. Remaining: implement the versioned progress service, previous/baseline deltas, metric intersections, `non_normalised` safeguards, confidence/comparability metadata, and API payload.

[~] **User specified improvement** User is able to specify fields of improvement (1 or more) (multi select choice) when project is created. However, if score > 80 for a chosen metric, feedback to user that they are proficient enough in this area and should work on other identified areas (lowest score highest prio)

[~] **Feedback for stagnant progress — logic implemented.** Done: `backend/itspeak/progress.py` compares a session to its reference and, per shared metric, flags any gain below the configurable `ITSPEAK_STAGNATION_MIN_DELTA` (default 5) as `stagnant`, and any regression as `declining`, producing "you're not improving in this metric" feedback. Comparability is protected: no reference (baseline), a changed archetype, or missing optional metrics return no false signals. The signals are attached to the coaching report (`CoachingReport.stagnation`) and covered by `tests/test_stagnation.py`. Remaining: drive the comparison off the actual previous comparable session once the progress service lands (currently uses the baseline reference), surface the signals in the progress UI, and attach a relevant practice video link from our database.

[~] **Implement Best Session Replay and the Coaching Playbook — in progress.** Done: retained sessions store aggregate scores/confidence and coaching cards have stable IDs, source-session links, source metrics, timestamps, and per-session fingerprint deduplication. Remaining: implement winner selection/recomputation, authenticated replay links, severity/archetype metadata, playbook persistence, explicit save/remove, and cross-session deduplication.

[~] **Harden concurrency, privacy, and recovery behavior — in progress.** Done: the commit path uses row locks and a database transaction, Session 1 protection exists below the UI, RLS ownership policies and a private artifact bucket are defined, temporary cleanup runs periodically, and baseline/commit/replacement/cleanup events are recorded. Remaining: activate Clerk-authenticated ownership, verify RLS with real user tokens, add idempotency and reset/playbook/archetype audit events, reconcile orphaned durable objects, and define/test both database/object-store failure directions.

[~] **Verify the complete lifecycle before enabling the feature — in progress.** Done: 20 backend tests and 9 frontend tests pass, including Clerk token acceptance/rejection, missing-auth fail-closed behavior, owner isolation, baseline assignment, the five-session boundary, project CRUD, authenticated API headers, replacement filtering, schema parity, artifact access, and analysis contracts; the current Next.js production build passes, and the live Supabase migration/function/pgcrypto configuration is verified. Remaining: add the listed concurrency, stale replacement, reset, progress, stagnation, replay, playbook, RLS, cleanup, and frontend lifecycle cases, then run both same- and changed-archetype Sessions 1-6 end-to-end scenarios.

[ ] vocial variety rename to vocab variety  